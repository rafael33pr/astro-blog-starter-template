<!-- SECCIÓN PPA SUNRUN - Datos Corregidos -->
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=5.0">
<!-- EMBUDO PPA SUNRUN + BATERÍA - Fuente Montserrat -->
<section class="sunrun-funnel" id="ppa-sunrun">
    
    <!-- GANCHO 1: Problema -->
    <div class="funnel-section problem-section">
        <div class="container">
            <span class="eyebrow">⚡ Clientes de LUMA en Puerto Rico</span>
            <h2>Tu factura subió este mes. Y volverá a subir el próximo.</h2>
            <p class="lead">Hay una salida: energía solar con batería de respaldo, sin que compres nada, instales nada ni pagues nada hoy.</p>
            <div class="scroll-hint">
                <span>Sigue leyendo ↓</span>
                <div class="bounce-arrow"></div>
            </div>
        </div>
    </div>

    <!-- GANCHO 2: Solución con batería -->
    <div class="funnel-section solution-section">
        <div class="container">
            <div class="sunrun-brand">
                <span class="powered">Presentado por</span>
                <a href="#" target="_blank" class="sunrun-logo-link">
                    <span class="sunrun-name">sunrun</span>
                    <span class="external">↗</span>
                </a>
                <span class="verified">✓ +600.000 sistemas con batería en EE.UU.</span>
            </div>
            
            <h3>Solar + Batería: Generas tu luz y la guardas para cuando LUMA se va</h3>
            
            <div class="battery-highlight">
                <span class="battery-icon">🔋</span>
                <div class="battery-text">
                    <strong>TODOS los sistemas PPA incluyen batería</strong>
                    <p>Cuando LUMA falla, tu casa sigue con luz. Cuando hay sol, guardas energía. Cuando no hay sol, usas lo guardado.</p>
                </div>
            </div>
            
            <div class="how-it-works-simple">
                <div class="simple-step">
                    <span class="icon">☀️</span>
                    <p>Paneles captan sol</p>
                    <span class="detail">De día: usas energia solar </span>
                </div>
                <div class="arrow">→</div>
                <div class="simple-step highlight">
                    <span class="icon">🔋</span>
                    <p>Batería guarda el excedente</p>
                    <span class="detail">De noche: usas tu batería</span>
                </div>
                <div class="arrow">→</div>
                <div class="simple-step">
                    <span class="icon">🏠</span>
                    <p>Casa con luz 24/7</p>
                    <span class="detail">Apagón de LUMA: tú tienes luz</span>
                </div>
            </div>
            
            <p class="key-difference"><strong>La diferencia:</strong> Con LUMA pagas por una red que falla. Con Sunrun PPA tienes paneles + batería propios, luz más barata, y respaldo cuando LUMA se va.</p>
        </div>
    </div>

    <!-- GANCHO 3: Comparación con batería -->
    <div class="funnel-section comparison-section">
        <div class="container">
            <h3>15 años comparados: ¿Qué tienes al final?</h3>
            
            <div class="math-showdown">
                <div class="math-card luma-math">
                    <div class="math-header">
                        <span>LUMA Energy</span>
                        <span class="trend">📈 Subiendo</span>
                    </div>
                    <div class="math-body">
                        <div class="feature-row bad">
                            <span>❌</span>
                            <span>Pagas luz cara</span>
                        </div>
                        <div class="feature-row bad">
                            <span>❌</span>
                            <span>Sin batería de respaldo</span>
                        </div>
                        <div class="feature-row bad">
                            <span>❌</span>
                            <span>Apagones incluidos</span>
                        </div>
                        <div class="feature-row bad">
                            <span>❌</span>
                            <span>Nada es tuyo</span>
                        </div>
                    </div>
                    <div class="year-highlight">
                        <span>Año 15:</span>
                        <span class="price">~$260/mes</span>
                    </div>
                    <div class="math-total">
                        <span>Total quemado:</span>
                        <span class="amount bad">~$35,000</span>
                    </div>
                    <div class="you-have">
                        <span>Al final tienes:</span>
                        <span class="nothing">Nada. Sigues pagando.</span>
                    </div>
                </div>

                <div class="vs-center">
                    <span class="vs-text">VS</span>
                    <div class="savings-tag">
                        <span>Con batería incluida</span>
                    </div>
                </div>

                <div class="math-card sunrun-math">
                    <div class="math-header good">
                        <span>Sunrun PPA</span>
                        <span class="trend">🔒 Fijo 15 años</span>
                    </div>
                    <div class="math-body">
                        <div class="feature-row good">
                            <span>✓</span>
                            <span>Pagas menos que LUMA</span>
                        </div>
                        <div class="feature-row good">
                            <span>✓</span>
                            <span><strong>Batería de respaldo incluida</strong></span>
                        </div>
                        <div class="feature-row good">
                            <span>✓</span>
                            <span>Luz durante apagones</span>
                        </div>
                        <div class="feature-row good">
                            <span>✓</span>
                            <span>Medicion Neta</span>
                        </div>
                    </div>
                    <div class="year-highlight good">
                        <span>Año 15:</span>
                        <span class="price">Precio fijo</span>
                    </div>
                    <div class="math-total">
                        <span>Protección total:</span>
                        <span class="amount good">15 años</span>
                    </div>
                    <div class="you-have good">
                        <span>Al final:</span>
                        <span class="options">Opción a renovar, comprar o remover</span>
                    </div>
                </div>
            </div>
            
            <p class="boricua-hook">En Puerto Rico, donde los apagones son norma, ¿por qué pagar por luz que se va cuando puedes tener luz que se queda?</p>
        </div>
    </div>

    <!-- GANCHO 4: Batería es clave -->
    <div class="funnel-section battery-section">
        <div class="container">
            <h3>La batería no es extra. Es el punto.</h3>
            
            <div class="battery-scenarios">
                <div class="scenario">
                    <div class="scenario-time">🌙 8:00 PM</div>
                    <p>Sin batería: LUMA se fue, estás a oscuras</p>
                    <p class="with-battery"><strong>Con batería:</strong> Tu casa brilla, TV encendida, nevera fría</p>
                </div>
                <div class="scenario">
                    <div class="scenario-time">⛈️ Huracán</div>
                    <p>Sin batería: Semanas sin luz esperando a LUMA</p>
                    <p class="with-battery"><strong>Con batería:</strong> Día 1, día 10, día 30: tú tienes luz</p>
                </div>
                <div class="scenario">
                    <div class="scenario-time">💰 Factura</div>
                    <p>Sin batería: Pagas todo a LUMA</p>
                    <p class="with-battery"><strong>Con batería:</strong> Pagas menos, usas tu energía guardada</p>
                </div>
            </div>
            
            <div class="battery-included-box">
                <span class="check-big">✓</span>
                <div>
                    <strong>Batería incluida en TODOS los PPA de Sunrun</strong>
                    <p>No es upgrade. No cuesta extra. Viene con el sistema porque sin batería no tiene sentido.</p>
                </div>
            </div>
        </div>
    </div>

    <!-- GANCHO 5: $0 inversión -->
    <div class="funnel-section objection-section">
        <div class="container">
            <h3>"¿Cuánto cuesta la batería?" — Viene incluida. $0 extra.</h3>
            
            <div class="zero-investment">
                <div class="zero-item">
                    <span class="zero-x">$0</span>
                    <span class="zero-label">Paneles solares</span>
                </div>
                <div class="zero-item highlight">
                    <span class="zero-x">$0</span>
                    <span class="zero-label">Batería de respaldo</span>
                </div>
                <div class="zero-item">
                    <span class="zero-x">$0</span>
                    <span class="zero-label">Instalación</span>
                </div>

            </div>
            
            <p class="sunrun-promise">Sunrun pone todo: paneles + batería + instalación. <strong>Tú pagas solo la energía que produces, a precio menor que LUMA.</strong></p>
        </div>
    </div>

    <!-- GANCHO 6: Urgencia boricua -->
    <div class="funnel-section urgency-section">
        <div class="container">
            <div class="pr-alert">
                <span class="alert-icon">🇵🇷</span>
                <div class="alert-text">
                    <h4>Puerto Rico: luz más cara de EE.UU. + apagones frecuentes</h4>
                    <p>La batería no es lujo aquí. Es necesidad. Sunrun la incluye porque saben que sin ella, no funciona.</p>
                </div>
            </div>
            
            <div class="timeline-urgency">
                <p class="timeline-q">¿Cuánto le has regalado a LUMA?</p>
                <div class="timeline-options">
                    <button class="timeline-btn" onclick="showLoss(1)">1 año → ~$1,500</button>
                    <button class="timeline-btn" onclick="showLoss(5)">5 años → ~$8,000</button>
                    <button class="timeline-btn" onclick="showLoss(10)">10 años → ~$18,000</button>
                </div>
                <p class="timeline-result" id="loss-result">Con ese dinero ya tendrías años de luz solar + batería incluida.</p>
            </div>
        </div>
    </div>

    <!-- CTA FINAL -->
    <div class="funnel-section cta-section">
        <div class="container">
            <h2>Solar + Batería. Sin comprar. Solo cambiar de LUMA a Sunrun.</h2>
            <p class="cta-lead">Verifica si tu casa califica. Evaluación gratis, respuesta en 24h.</p>
            
            <a href="https://wa.me/17879729553?text=Hola%20Rafael,%20quiero%20saber%20si%20califico%20para%20PPA%20de%20Sunrun%20con%20bateria" class="mega-cta">
                <span class="cta-main">Verificar si califico →</span>
                <span class="cta-sub">Solar + batería incluida • Sin inversión inicial</span>
            </a>
            
            <div class="final-reassurance">
                <p>✓ Revisamos tu techo y factura LUMA</p>
                <p>✓ Te decimos si cabe solar + batería</p>
                <p>✓ Solo procedes si te convence</p>
            </div>
            
            <p class="micro-copy">Consultor Pura Energia. Sunrun es marca registrada. <a href="https://www.sunrun.com" target="_blank">sunrun.com</a></p>
        </div>
    </div>

</section>

<style>
* {
    box-sizing: border-box;
}

html {
    font-size: 16px;
    -webkit-text-size-adjust: 100%;
    -ms-text-size-adjust: 100%;
}

body {
    margin: 0;
    padding: 0;
    font-family: 'Montserrat', sans-serif;
    overflow-x: hidden;
}

/* FUENTES RESPONSIVAS - Base para móvil */
.sunrun-funnel {
    --pe-yellow: #FBBF24;
    --pe-orange: #F97316;
    --pe-orange-dark: #EA580C;
    --pe-blue: #0369A1;
    --pe-dark: #0C4A6E;
    --pe-cream: #FEF3C7;
    --pe-light: #FFFBEB;
    
    font-size: clamp(14px, 4vw, 18px);
    line-height: 1.6;
}

/* CONTENEDOR FULL WIDTH EN MÓVIL */
.sunrun-funnel .container {
    width: 100%;
    max-width: 100%;
    margin: 0 auto;
    padding: 0 4vw;
}

/* ========== SECCIÓN 1: PROBLEMA ========== */
.problem-section {
    background: linear-gradient(135deg, var(--pe-orange-dark) 0%, var(--pe-orange) 100%);
    color: white;
    padding: 8vw 0;
    text-align: center;
    width: 100%;
}

.eyebrow {
    display: inline-block;
    background: rgba(255,255,255,0.2);
    padding: 2vw 4vw;
    border-radius: 50px;
    font-size: clamp(0.7rem, 3vw, 0.9rem);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 1px;
    margin-bottom: 4vw;
}

.problem-section h2 {
    font-size: clamp(1.5rem, 6vw, 2.5rem);
    font-weight: 800;
    margin-bottom: 3vw;
    line-height: 1.2;
    padding: 0 2vw;
}

.problem-section .lead {
    font-size: clamp(1rem, 4.5vw, 1.3rem);
    font-weight: 500;
    opacity: 0.95;
    max-width: 90%;
    margin: 0 auto 4vw;
    line-height: 1.5;
}

.scroll-hint {
    margin-top: 4vw;
    opacity: 0.8;
    font-size: clamp(0.8rem, 3vw, 1rem);
}

.bounce-arrow {
    width: 5vw;
    height: 5vw;
    max-width: 25px;
    max-height: 25px;
    border-right: 3px solid white;
    border-bottom: 3px solid white;
    transform: rotate(45deg);
    margin: 3vw auto 0;
    animation: bounce 2s infinite;
}

@keyframes bounce {
    0%, 20%, 50%, 80%, 100% { transform: translateY(0) rotate(45deg); }
    40% { transform: translateY(-10px) rotate(45deg); }
    60% { transform: translateY(-5px) rotate(45deg); }
}

/* ========== SECCIÓN 2: SOLUCIÓN ========== */
.solution-section {
    background: var(--pe-cream);
    padding: 8vw 0;
    text-align: center;
    width: 100%;
}

.sunrun-brand {
    margin-bottom: 5vw;
}

.powered {
    display: block;
    font-size: clamp(0.7rem, 2.5vw, 0.85rem);
    font-weight: 500;
    color: var(--pe-orange-dark);
    margin-bottom: 2vw;
    text-transform: uppercase;
    letter-spacing: 1px;
}

.sunrun-logo-link {
    display: inline-flex;
    align-items: center;
    gap: 1vw;
    text-decoration: none;
    color: var(--pe-blue);
    font-size: clamp(1.8rem, 8vw, 3rem);
    font-weight: 800;
    letter-spacing: -2px;
}

.external {
    font-size: clamp(0.8rem, 3vw, 1rem);
    color: var(--pe-orange);
    font-weight: 600;
}

.verified {
    display: block;
    font-size: clamp(0.7rem, 2.5vw, 0.8rem);
    color: #16A34A;
    margin-top: 2vw;
    font-weight: 600;
}

.solution-section h3 {
    font-size: clamp(1.2rem, 5vw, 1.8rem);
    color: var(--pe-dark);
    margin-bottom: 5vw;
    font-weight: 700;
    padding: 0 3vw;
}

/* BATERÍA DESTACADA */
.battery-highlight {
    display: flex;
    align-items: center;
    gap: 3vw;
    background: white;
    padding: 4vw;
    border-radius: 3vw;
    border-left: 4px solid var(--pe-orange);
    margin: 0 2vw 5vw;
    text-align: left;
}

.battery-icon {
    font-size: clamp(2rem, 10vw, 4rem);
    flex-shrink: 0;
}

.battery-text strong {
    display: block;
    color: var(--pe-orange-dark);
    margin-bottom: 1vw;
    font-weight: 700;
    font-size: clamp(0.95rem, 4vw, 1.1rem);
}

.battery-text p {
    color: var(--pe-dark);
    margin: 0;
    font-size: clamp(0.85rem, 3.5vw, 1rem);
    font-weight: 500;
    line-height: 1.5;
}

/* PASOS */
.how-it-works-simple {
    display: flex;
    flex-direction: column;
    gap: 4vw;
    margin-bottom: 5vw;
}

.simple-step {
    background: white;
    padding: 5vw;
    border-radius: 3vw;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    width: 90%;
    max-width: 400px;
    margin: 0 auto;
    border: 2px solid transparent;
}

.simple-step.highlight {
    border-color: var(--pe-orange);
    background: var(--pe-light);
}

.simple-step .icon {
    font-size: clamp(2.5rem, 12vw, 4rem);
    display: block;
    margin-bottom: 2vw;
}

.simple-step p {
    font-weight: 700;
    color: var(--pe-dark);
    margin-bottom: 1vw;
    font-size: clamp(1.1rem, 5vw, 1.3rem);
}

.simple-step .detail {
    font-size: clamp(0.9rem, 4vw, 1.1rem);
    color: var(--pe-orange-dark);
    font-weight: 600;
}

.arrow {
    font-size: clamp(1.5rem, 8vw, 2.5rem);
    color: var(--pe-orange);
    font-weight: 700;
    transform: rotate(90deg);
}

.key-difference {
    background: white;
    padding: 4vw;
    border-radius: 3vw;
    border-left: 4px solid var(--pe-orange);
    text-align: left;
    font-size: clamp(0.95rem, 4vw, 1.15rem);
    margin: 0 2vw;
    font-weight: 500;
    line-height: 1.6;
}

.key-difference strong {
    color: var(--pe-orange-dark);
    font-weight: 700;
}

/* ========== SECCIÓN 3: COMPARACIÓN ========== */
.comparison-section {
    background: white;
    padding: 8vw 0;
    width: 100%;
}

.comparison-section h3 {
    text-align: center;
    font-size: clamp(1.3rem, 5.5vw, 1.8rem);
    margin-bottom: 5vw;
    color: var(--pe-dark);
    font-weight: 700;
    padding: 0 3vw;
}

.math-showdown {
    display: flex;
    flex-direction: column;
    gap: 4vw;
}

.math-card {
    background: white;
    border-radius: 4vw;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0,0,0,0.15);
    margin: 0 2vw;
}

.math-header {
    padding: 3vw 4vw;
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-weight: 700;
    font-size: clamp(0.9rem, 4vw, 1.1rem);
}

.luma-math .math-header {
    background: #FEE2E2;
    color: #991B1B;
}

.sunrun-math .math-header {
    background: var(--pe-cream);
    color: var(--pe-orange-dark);
}

.trend {
    font-size: clamp(0.65rem, 2.5vw, 0.8rem);
    padding: 1.5vw 3vw;
    border-radius: 12px;
    background: rgba(255,255,255,0.5);
    font-weight: 600;
}

.math-body {
    padding: 4vw;
}

.feature-row {
    display: flex;
    gap: 2vw;
    padding: 3vw 0;
    border-bottom: 1px solid #E5E7EB;
    font-size: clamp(0.95rem, 4vw, 1.1rem);
    font-weight: 500;
    align-items: center;
}

.feature-row:last-child {
    border-bottom: none;
}

.feature-row.bad {
    color: #991B1B;
}

.feature-row.good {
    color: var(--pe-dark);
}

.feature-row.good strong {
    color: var(--pe-orange-dark);
    font-weight: 700;
}

.year-highlight {
    padding: 3vw 4vw;
    background: #F9FAFB;
    display: flex;
    justify-content: space-between;
    font-weight: 600;
    border-top: 2px solid #E5E7EB;
    font-size: clamp(0.95rem, 4vw, 1.1rem);
    align-items: center;
}

.year-highlight.good {
    background: var(--pe-light);
    border-color: var(--pe-yellow);
}

.year-highlight .price {
    font-weight: 700;
}

.year-highlight .price.bad {
    color: #DC2626;
    font-size: clamp(1.1rem, 5vw, 1.3rem);
}

.year-highlight .price.good {
    color: var(--pe-orange-dark);
}

.math-total {
    padding: 3vw 4vw;
    background: #F3F4F6;
    display: flex;
    justify-content: space-between;
    font-weight: 700;
    font-size: clamp(1rem, 4.5vw, 1.2rem);
    align-items: center;
}

.amount.bad {
    color: #DC2626;
    font-size: clamp(1.2rem, 6vw, 1.5rem);
    font-weight: 800;
}

.amount.good {
    color: var(--pe-orange-dark);
    font-size: clamp(1.2rem, 6vw, 1.5rem);
    font-weight: 800;
}

.you-have {
    padding: 3vw 4vw;
    background: white;
    text-align: center;
    font-size: clamp(0.85rem, 3.5vw, 1rem);
}

.you-have span:first-child {
    display: block;
    color: #6B7280;
    margin-bottom: 1vw;
    font-weight: 500;
}

.nothing {
    color: #DC2626;
    font-weight: 700;
    font-size: clamp(1rem, 4.5vw, 1.2rem);
}

.you-have.good .options {
    color: var(--pe-orange-dark);
    font-weight: 700;
    font-size: clamp(1rem, 4.5vw, 1.2rem);
}

.vs-center {
    text-align: center;
    padding: 2vw 0;
}

.vs-text {
    display: block;
    font-size: clamp(1.3rem, 6vw, 1.8rem);
    font-weight: 800;
    color: #9CA3AF;
    margin-bottom: 2vw;
}

.savings-tag {
    background: var(--pe-orange);
    color: white;
    padding: 3vw 5vw;
    border-radius: 3vw;
    font-weight: 700;
    font-size: clamp(0.85rem, 3.5vw, 1rem);
    display: inline-block;
}

.boricua-hook {
    text-align: center;
    font-size: clamp(1.05rem, 4.5vw, 1.3rem);
    color: var(--pe-orange-dark);
    font-style: italic;
    margin: 5vw 3vw 0;
    font-weight: 500;
    line-height: 1.5;
}

/* ========== SECCIÓN 4: BATERÍA DETALLE ========== */
.battery-section {
    background: var(--pe-light);
    padding: 8vw 0;
    width: 100%;
}

.battery-section h3 {
    text-align: center;
    font-size: clamp(1.3rem, 5.5vw, 1.8rem);
    color: var(--pe-dark);
    margin-bottom: 5vw;
    font-weight: 700;
    padding: 0 3vw;
}

.battery-scenarios {
    display: flex;
    flex-direction: column;
    gap: 4vw;
    margin-bottom: 5vw;
}

.scenario {
    background: white;
    padding: 5vw;
    border-radius: 3vw;
    box-shadow: 0 4px 15px rgba(0,0,0,0.08);
    margin: 0 3vw;
}

.scenario-time {
    font-size: clamp(1.2rem, 5.5vw, 1.5rem);
    font-weight: 700;
    color: var(--pe-orange-dark);
    margin-bottom: 2vw;
}

.scenario p {
    color: #6B7280;
    font-size: clamp(0.95rem, 4vw, 1.1rem);
    margin-bottom: 2vw;
    font-weight: 500;
    line-height: 1.5;
}

.with-battery {
    color: var(--pe-dark) !important;
    font-weight: 600 !important;
    font-size: clamp(1rem, 4.5vw, 1.15rem) !important;
}

.battery-included-box {
    display: flex;
    align-items: center;
    gap: 3vw;
    background: linear-gradient(135deg, var(--pe-orange), var(--pe-orange-dark));
    color: white;
    padding: 5vw;
    border-radius: 3vw;
    margin: 0 3vw;
}

.check-big {
    font-size: clamp(2rem, 10vw, 3.5rem);
    font-weight: 700;
    flex-shrink: 0;
}

.battery-included-box strong {
    display: block;
    margin-bottom: 1vw;
    font-weight: 700;
    font-size: clamp(1rem, 4.5vw, 1.2rem);
}

.battery-included-box p {
    margin: 0;
    font-size: clamp(0.85rem, 3.5vw, 1rem);
    opacity: 0.95;
    font-weight: 500;
    line-height: 1.5;
}

/* ========== SECCIÓN 5: $0 INVERSIÓN ========== */
.objection-section {
    background: white;
    padding: 8vw 0;
    text-align: center;
    width: 100%;
}

.objection-section h3 {
    font-size: clamp(1.15rem, 5vw, 1.5rem);
    margin-bottom: 5vw;
    color: var(--pe-dark);
    max-width: 90%;
    margin-left: auto;
    margin-right: auto;
    font-weight: 700;
    line-height: 1.4;
}

.zero-investment {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3vw;
    margin: 0 3vw 5vw;
}

.zero-item {
    background: var(--pe-light);
    padding: 4vw;
    border-radius: 3vw;
    border: 2px solid transparent;
}

.zero-item.highlight {
    border-color: var(--pe-orange);
    background: white;
    box-shadow: 0 4px 20px rgba(249,115,22,0.2);
}

.zero-x {
    display: block;
    font-size: clamp(2rem, 10vw, 3rem);
    font-weight: 800;
    color: var(--pe-orange-dark);
    margin-bottom: 1vw;
}

.zero-label {
    font-size: clamp(0.8rem, 3.5vw, 0.95rem);
    color: var(--pe-dark);
    font-weight: 600;
    line-height: 1.3;
}

.sunrun-promise {
    font-size: clamp(1rem, 4.5vw, 1.2rem);
    max-width: 90%;
    margin: 0 auto;
    color: var(--pe-dark);
    font-weight: 500;
    line-height: 1.6;
    padding: 0 3vw;
}

.sunrun-promise strong {
    color: var(--pe-orange-dark);
    font-weight: 700;
}

/* ========== SECCIÓN 6: URGENCIA ========== */
.urgency-section {
    background: var(--pe-dark);
    color: white;
    padding: 8vw 0;
    width: 100%;
}

.pr-alert {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3vw;
    background: rgba(251,191,36,0.2);
    padding: 5vw;
    border-radius: 3vw;
    margin: 0 3vw 5vw;
    border: 2px solid var(--pe-yellow);
    text-align: center;
}

.alert-icon {
    font-size: clamp(2.5rem, 12vw, 4rem);
}

.alert-text h4 {
    font-size: clamp(1.1rem, 5vw, 1.4rem);
    margin-bottom: 2vw;
    color: var(--pe-yellow);
    font-weight: 700;
    line-height: 1.3;
}

.alert-text p {
    opacity: 0.9;
    margin: 0;
    font-size: clamp(0.95rem, 4vw, 1.15rem);
    font-weight: 500;
    line-height: 1.5;
}

.timeline-urgency {
    text-align: center;
}

.timeline-q {
    font-size: clamp(1.2rem, 5.5vw, 1.5rem);
    margin-bottom: 4vw;
    font-weight: 600;
    padding: 0 3vw;
}

.timeline-options {
    display: flex;
    flex-direction: column;
    gap: 2vw;
    margin-bottom: 4vw;
    padding: 0 4vw;
}

.timeline-btn {
    background: rgba(255,255,255,0.1);
    border: 2px solid var(--pe-yellow);
    color: white;
    padding: 4vw;
    border-radius: 3vw;
    cursor: pointer;
    font-size: clamp(1rem, 4.5vw, 1.2rem);
    font-family: 'Montserrat', sans-serif;
    font-weight: 600;
    width: 100%;
    max-width: 400px;
    margin: 0 auto;
}

.timeline-btn:hover {
    background: var(--pe-yellow);
    color: var(--pe-dark);
}

.timeline-result {
    font-size: clamp(1.1rem, 5vw, 1.4rem);
    font-weight: 600;
    min-height: 1.5em;
    color: var(--pe-yellow);
    padding: 0 3vw;
    line-height: 1.5;
}

/* ========== SECCIÓN 7: CTA ========== */
.cta-section {
    background: linear-gradient(135deg, var(--pe-blue) 0%, var(--pe-dark) 100%);
    color: white;
    padding: 8vw 0;
    text-align: center;
    width: 100%;
}

.cta-section h2 {
    font-size: clamp(1.3rem, 6vw, 2rem);
    margin-bottom: 3vw;
    font-weight: 700;
    padding: 0 3vw;
    line-height: 1.3;
}

.cta-lead {
    font-size: clamp(1.05rem, 4.5vw, 1.3rem);
    opacity: 0.95;
    max-width: 90%;
    margin: 0 auto 5vw;
    font-weight: 500;
    line-height: 1.5;
    padding: 0 3vw;
}

.mega-cta {
    display: block;
    background: var(--pe-yellow);
    color: var(--pe-dark);
    padding: 5vw 6vw;
    border-radius: 3vw;
    text-decoration: none;
    margin: 0 4vw 5vw;
    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    transition: transform 0.2s;
}

.mega-cta:hover {
    transform: scale(1.02);
}

.cta-main {
    display: block;
    font-size: clamp(1.15rem, 5.5vw, 1.4rem);
    font-weight: 700;
    margin-bottom: 1vw;
    line-height: 1.3;
}

.cta-sub {
    display: block;
    font-size: clamp(0.85rem, 3.5vw, 1rem);
    color: var(--pe-dark);
    opacity: 0.8;
    font-weight: 500;
}

.final-reassurance {
    margin-bottom: 5vw;
    padding: 0 3vw;
}

.final-reassurance p {
    margin: 2vw 0;
    font-size: clamp(0.95rem, 4vw, 1.1rem);
    opacity: 0.9;
    font-weight: 500;
}

.micro-copy {
    font-size: clamp(0.7rem, 3vw, 0.85rem);
    opacity: 0.7;
    padding: 0 4vw;
}

.micro-copy a {
    color: var(--pe-yellow);
    text-decoration: underline;
    font-weight: 600;
}

/* ============================================
   DESKTOP - Vistas grandes
   ============================================ */
@media (min-width: 769px) {
    .sunrun-funnel {
        font-size: 16px;
    }
    
    .sunrun-funnel .container {
        max-width: 800px;
        padding: 0 2rem;
    }
    
    .problem-section,
    .solution-section,
    .comparison-section,
    .battery-section,
    .objection-section,
    .urgency-section,
    .cta-section {
        padding: 4rem 0;
    }
    
    .how-it-works-simple {
        flex-direction: row;
    }
    
    .arrow {
        transform: rotate(0deg);
    }
    
    .simple-step {
        width: 200px;
        padding: 1.5rem;
    }
    
    .math-showdown {
        display: grid;
        grid-template-columns: 1fr auto 1fr;
        gap: 1.5rem;
    }
    
    .vs-center {
        align-self: center;
    }
    
    .battery-scenarios {
        flex-direction: row;
        display: grid;
        grid-template-columns: repeat(3, 1fr);
    }
    
    .scenario {
        margin: 0;
    }
    
    .zero-investment {
        display: flex;
        justify-content: center;
        flex-wrap: wrap;
    }
    
    .zero-item {
        width: 160px;
        padding: 1.5rem;
    }
    
    .timeline-options {
        flex-direction: row;
        justify-content: center;
    }
    
    .timeline-btn {
        width: auto;
        padding: 1rem 2rem;
    }
    
    .mega-cta {
        max-width: 400px;
        margin: 0 auto 2rem;
        padding: 1.5rem 2rem;
    }
}
</style>

<script>
function showLoss(years) {
    const losses = {
        1: "Con $1,500 ya tendrías meses de solar + batería incluida",
        5: "Con $8,000 quemados, ya tendrías años de luz propia",
        10: "Con $18,000, hoy tu sistema estaría casi pagado"
    };
    
    const result = document.getElementById('loss-result');
    result.textContent = losses[years];
}
</script>
/* ARREGLO ESPECÍFICO PARA 1920x1080 Y DESKTOP */
@media (min-width: 1200px) {
    .math-showdown {
        display: grid;
        grid-template-columns: 1fr 80px 1fr;
        gap: 2rem;
        align-items: stretch;
        max-width: 1000px;
        margin: 0 auto;
    }
    
    .math-card {
        min-width: 380px;
        max-width: 450px;
    }
    
    .math-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 1.2rem 1.5rem;
        min-height: 70px;
        gap: 1.5rem;
    }
    
    .math-header span:first-child {
        font-size: 1.3rem;
        font-weight: 700;
        white-space: nowrap;
    }
    
    .math-header .trend {
        font-size: 0.85rem;
        padding: 0.5rem 1rem;
        white-space: nowrap;
        border-radius: 20px;
    }
    
    .vs-center {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100%;
    }
    
    .vs-text {
        font-size: 1.5rem;
        margin-bottom: 1rem;
    }
    
    .savings-tag {
        padding: 1rem 1.5rem;
        font-size: 1rem;
    }
}

/* PARA PANTALLAS MEDIANAS (1024px - 1199px) */
@media (min-width: 1024px) and (max-width: 1199px) {
    .math-showdown {
        gap: 1.5rem;
    }
    
    .math-card {
        min-width: 320px;
    }
    
    .math-header {
        padding: 1rem;
        gap: 1rem;
    }
    
    .math-header span:first-child {
        font-size: 1.1rem;
    }
}
</style>
</style>

<script>
function showLoss(years) {
    const losses = {
        1: "Con $1,500 ya tendrías meses de solar + batería incluida",
        5: "Con $8,000 quemados, ya tendrías años de luz propia",
        10: "Con $18,000, hoy tu sistema estaría casi pagado"
    };
    
    const result = document.getElementById('loss-result');
    result.textContent = losses[years];
}
</script>
